name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/excel-addin

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: apps/excel-addin/package-lock.json

      - run: npm ci

      # Nothing outside src/edition/<name>/ reaches inside it, and src/core
      # never imports @edition. See CLAUDE.md, Editions.
      - name: Edition boundary
        run: npm run check:boundary

      # tsc is the real correctness gate. Vite's build tolerates type errors
      # that tsc catches, so run this first and separately.
      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm test

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

      # manifest.xml is generated, so build it before validating it.
      - name: Validate manifest
        run: |
          npm run manifest:dev
          npm run validate
