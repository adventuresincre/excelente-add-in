Office.onReady(() => {
  // Ribbon command handlers register here via Office.actions.associate.
  // None defined yet — manifest's FunctionFile points to commands.html
  // but no commands are wired to UI controls in Phase 0.
});
